const inquirer = require("inquirer");
const fs = require("fs"); 

const init = function () {
    inquirer
      .prompt([
  {
    type:"input",
    message: "What is the manager\'s name?",
    name: "userName",
  }
      ])
.then(

)
}
module.exports = Generator;